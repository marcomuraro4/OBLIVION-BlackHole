float3 CameraPos = LWCToFloat(ResolvedView.WorldCameraOrigin);
float3 CameraDir = - Parameters.CameraVector;

Dir = CameraDir;
Mask = 1.0;
BlackHoleProperties BHP;
BHP.Mass = Mass;
BHP.EventHorizon = Mass*2;

BlackHole(CameraPos, Dir, Mask, BHP);

return 1.0;